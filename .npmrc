# Producción (Railway): instalar solo dependencias de runtime.
# `puppeteer` es una herramienta local de capturas (devDependency) y su postinstall
# descarga Chromium (~170MB) — innecesario y arriesgado en el build de Railway.
omit=dev
