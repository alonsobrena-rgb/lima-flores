# Producción (Railway): instalar solo dependencias de runtime.
omit=dev

# El skip de descarga de Chrome de puppeteer se hace en .puppeteerrc.cjs
# (skipDownload). npm ignora el config puppeteer_skip_download en .npmrc.
