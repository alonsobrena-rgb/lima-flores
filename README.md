# Lima Flores

Sitio del atelier botánico Lima Flores — catálogo, página de producto, checkout y suscripción de flores.

Sitio estático servido con `serve` (Node). Deploy en Railway vía Nixpacks (`npm start`).

## Desarrollo local
```
npm install
npm run dev   # http://localhost:3000
```
