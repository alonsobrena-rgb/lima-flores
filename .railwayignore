node_modules
.git
.railway
npm-debug.log*
.DS_Store
.env

# Sube solo lo que el runtime necesita. `tools/` son ~340 MB de capturas de
# pruebas y reventaba el límite de subida de `railway up`.
#
# OJO: `app/` NO va acá. El deploy de GitHub compila la tienda React
# (railway.json → `cd app && npm run build`) y `server.js` sirve `app/dist`; sin
# app/ no hay frontend. Se excluía cuando el frontend era el sitio vanilla, que
# ya no existe.
tools/
.claude/
integrations/higgsfield/out/
integrations/cards/out/
